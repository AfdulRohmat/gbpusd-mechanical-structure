# Optional shared data directory used by both GBPUSD research repositories.
# If omitted, this project uses its gitignored local data/ directory.
GBPUSD_SHARED_DATA_ROOT=/absolute/path/to/trading-data/gbpusd
